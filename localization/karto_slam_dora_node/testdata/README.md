# Dora Karto replay datasets

`synthetic_room.jsonl` is a deterministic software fixture, not real-car acceptance evidence.
Record the next physical mapping lap with `tools/record_dataset.py`; preserve raw system-clock timestamps and unmodified LaserScan/Odometry payloads.
